name: provider-svm-native
runtime: yaml
plugins:
  providers:
    - name: svm
      path: ../../bin

resources:
  myRandomResource:
    type: svm:Random
    properties:
      length: 24

outputs:
  output:
    value: ${myRandomResource.result}
